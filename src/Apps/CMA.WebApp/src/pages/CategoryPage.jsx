import PageContainer from '@/components/layout/PageContainer';

export default function CategoryPage() {
  return (
    <PageContainer>
      <h1 className="text-2xl font-bold text-text-primary mb-6">Category Explorer</h1>
      <p className="text-text-muted">Category explorer — implementation in Phase 1.</p>
    </PageContainer>
  );
}
